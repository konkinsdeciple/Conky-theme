conky.config = {
	alignment = 'top_left',
	background = false,
	color2 = '308CC6',
	cpu_avg_samples = 2,
	default_color = 'FFFFFF',
	double_buffer = true,
	font = 'Bitstream Vera Sans:size=12',
	gap_x = 25,
	gap_y = 20,
	minimum_width = 300,
	minimum_height = 400,
	no_buffers = true,
	own_window = true,
	own_window_type = 'override',
	own_window_transparent = true,
	update_interval = 1.0,
	use_xft = true,
	gap_x = 20,
	gap_y = 20,



own_window=true,
own_window_type="normal",
own_window_transparent=true,
own_window_hints="undecorated,below,sticky,skip_taskbar,skip_pager",

own_window_argb_visual=true,
own_window_argb_value=0,

}
conky.text = [[

#This is the clock module

${font Bitstream Vera Sans:size=60}${color2}\
${alignr}${time %H:%M}\
${font}${color}
]]
